using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameEvent : MonoBehaviour
{
    public const string ENEMY_HIT = "ENEMY_HIT";    //敌人被击中事件
    public const string SPEED_CHANGED = "SPEED_CHANGED";  //速度改变事件
    public const string PLAYER_HIT = "PLAYER_HIT";  //完结被击中事件

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
